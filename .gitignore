# --- SECRETS : ne JAMAIS pousser ---
.env

# --- environnement local ---
.venv/
.claude/
__pycache__/
.pytest_cache/
.ipynb_checkpoints/

# --- artefacts / journaux locaux ---
*.log
*.tmp
*.parquet
*.npy

# NOTE : le `*.csv` global a ete retire lors du regroupement autour de ai-company/.
# Les CSV locaux sont desormais ignores la ou ils naissent (ai-company/.gitignore :
# tools/cache/, tools/history/) — un `*.csv` a la racine masquait silencieusement
# tout jeu de donnees qu'on voudrait versionner.
