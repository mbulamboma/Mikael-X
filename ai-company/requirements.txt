# Agent trader FTMO SWING multi-strategies (LangChain + AWS Bedrock + MT5)
# LangChain 1.x REQUIS : `create_agent` y remplace AgentExecutor +
# create_tool_calling_agent, supprimes de langchain.agents en 1.0.
langchain>=1.0
langchain-aws>=1.0        # ChatBedrockConverse (API Converse, tool-calling)
langchain-core>=1.0
langgraph>=1.0            # graphe d'agent + GraphRecursionError (budget d'outils)
boto3>=1.34               # SDK AWS (credentials + Bedrock runtime)
MetaTrader5>=5.0.45       # Windows uniquement
pandas
numpy
requests                  # news : FRED (Fed) + GDELT + recherche/lecture web
beautifulsoup4            # facultatif : meilleure extraction du texte des pages web
python-dotenv
